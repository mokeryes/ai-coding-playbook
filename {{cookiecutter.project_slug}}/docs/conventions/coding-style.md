# 编码风格(强制约束)

> 阶段 1 占位文件。**阶段 2 会从 wework-opus `CLAUDE.md` § 7 抽取并扩充**。

## 当前要点(摘要)

- 清晰 > 优雅
- 显式 > 隐式
- 同步 > 异步(除非明确需要并发)
- 文件 ≤ 200 行,函数 ≤ 50 行
- 日志先行
- 注释中文,变量/函数/文件名英文

## TODO(阶段 2 填充)

- 每条原则的"为什么" + 反例
- 语言相关的最佳实践(`{{ cookiecutter.primary_language }}` 视项目而定)
- 命名约定、文件组织、import 顺序
